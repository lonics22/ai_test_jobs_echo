## 工作经历

<div class="entry-header">
  <span class="title">Gate.io <span class="role">· 测试开发</span></span>
  <span class="date">2024.07 — 2026.06</span>
</div>

- **主导 AI 需求分析引擎迭代**，实现多格式需求文档（PDF/Word/TXT）智能解析与业务实体提取，集成 LVM 视觉大模型识别文档内嵌图片与流程图。需求到用例产出效率提升约 70%，覆盖 10+ 项目模块。
  → 押题：cat-1 q8 三阶段管线 / q9 大模型幻觉处理

- **设计 AI 生成用例三阶段管线（生成→评审→改进）**，writer 模型生成初稿后 reviewer 模型检测幻觉与遗漏，AI 按评审意见自动修改后人工确认入库；实现流式 SSE 实时推送与自动续写机制处理 Token 截断。AI 生成用例评审通过率 85%，用例编写效率提升 60%。
  → 押题：cat-1 q8 三阶段管线设计 / q9 大模型幻觉处理

- **搭建 AI Agent UI 自动化框架**，基于 browser-use 实现 LLM 自主操控浏览器完成复杂 E2E 测试（登录→搜索→下单→支付）；针对性适配国产模型输出格式（11 处运行时补丁），设计四层幽灵点击检测体系（前置校验→操作校验→逻辑校验→重试兜底）。覆盖 200+ 条 Agent 用例跑批，成功率约 85%，人工干预率低于 10%。
  → 押题：cat-1 q1 AI Agent 完整流程 / q2 幽灵点击 / q3 执行稳定性

- **设计统一模型调度层 AIModelConfig**，按 writer / reviewer / vision / browser_use_text 等 8 种 role 管理模型配置，支持 DeepSeek v4 flash/pro、智谱等多模型灰度切换与故障迁移，单角色故障不影响全局。不同场景按复杂度选择最优模型，Token 消耗降低 40-60%。
  → 押题：cat-1 q6 模型选型和调度策略 / q4 Token 消耗优化

- **实现三引擎分层回归策略**，Selenium 覆盖 70% 高频回归（核心链路 + 资金安全），AI Agent 覆盖 30% 复杂 E2E 场景，Playwright 做跨浏览器兼容。共用 Allure 报告平台，Jenkins Pipeline 按标签路由执行器。核心链路回归通过率 98%，AI Agent 场景零脚本维护成本。
  → 押题：cat-1 q5 AI Agent 和三引擎协作

- **实施 Prompt 工程体系 R.O.L.E.S.T 框架**，统一管理 8 种 role 的系统提示词模板，支持灰度切换与版本回滚；通过反向示例注入和业务约束补充持续迭代，已迭代 7 个版本，评审通过率从 55% 提升到 85%。
  → 押题：cat-1 q10 Prompt 工程体系管理

- **设计 AI 失败根因分析模块**，聚合日志/报文/截图/console 错误，LLM 自动分类失败类型（环境问题/业务逻辑/测试数据），匹配历史相似失败辅助定位。定位时间从 15 分钟缩短到 3 分钟，准确率约 70%。
  → 押题：cat-2 q5 失败根因分析

- **搭建 CI/CD 质量门禁与分布式执行架构**，P0 用例通过率 <95% 阻断发布；AI 智能回归选择基于代码变更推荐影响范围用例，全量 2000+ 缩到 800+，缺陷检出率保持 95% 以上。分布式执行基于 Docker + Selenium Grid，24 路并行 40 分钟出结果。
  → 押题：cat-3 q1 CI/CD 并发架构 / q6 Docker + Selenium Grid

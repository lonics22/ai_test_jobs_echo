有做过的，其中 browser-use 的 UI 自动化就是 AI Agent 模式。基于 browser-use 库 + LangChain，核心是一个用户输入自然语言任务描述、执行 UI 自动化的 AI Agent。

第一步，用户输入的内容用 LLM 将内容拆解为一个个子步骤。
第二步，Agent 主循环：
① 获取当前浏览器状态
② LLM 决策下一步操作（点击、输入、滚动、断言...）
③ 调用工具执行浏览器操作
④ 回调 → 进度追踪
⑤ 重复直到任务完成或超时

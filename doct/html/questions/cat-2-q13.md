从资金安全、核心链路、异常容错三个维度按 P0/P1/P2 分层。

**P0 资金安全——** 资产精度验证、账务三端一致性（数据库余额 / 缓存余额 / 用户流水日志）、幂等校验（重复下单不导致资产多次变更）、异常补偿（清算到一半服务重启后的补偿逻辑）。

**P1 核心业务——** 开仓→持仓→平仓完整链路、资金费率 8 小时结算快照准确性、标记价格计算（防价格操纵）、强平触发条件（30+ 边界场景）。

**P2 异常性能——** 高并发下单/撤单/强清的数据一致性、行情源断了的降级容错、越权操作（A 操作 B 订单）。

每次发布 P0 全量自动过，P1 按变更影响范围选测。

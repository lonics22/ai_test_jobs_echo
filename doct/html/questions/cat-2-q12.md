搜索算法的测试我主要用这么几种方法：

**第一，AB 对比验证。** 搭建两套环境，老算法和新的算法并行，用同样的测试词集去跑，对比召回率和排序效果。

**第二，测试词集构建。** 覆盖核心词、长尾词、品牌词、错别字、同义词的测试集，大概有 500 多条，用来系统化评估搜索质量。

**第三，多级兜底策略。** 核心词匹配 → 相似度 → 品牌词推荐 → 模糊搜索，每一级都要验证是否正确触发。

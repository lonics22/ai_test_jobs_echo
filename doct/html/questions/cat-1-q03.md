因为 browser-use 本身不提供持久化，所以我是通过自定义方法和回调来跟踪的。LLM 自己在执行过程中上报状态，比如子任务完成、子任务失败、完成任务状态更新、全部任务完成等。在 Prompt 里强制要求 LLM 必须在每个子任务结束后调用这些标记动作：step 结束会触发回调，做三件事：

1. 检查是否超时或被用户取消（should_stop）
2. 解析 LLM 的 model_output，提取实际执行的操作和状态变化
3. 通过 emit_callback 推送到前端，用户能看到实时进度

LLM 流式生成测试用例 + 截断续写的边界问题，用 SSE 流式推送到前端。LLM 有 token 上限，复杂需求经常生到一半被截断。比如生成了 15 个用例，第 15 个只写到一半就断了。续写时 LLM 忘了前面写了什么，可能重复生成或者格式不一致。试过传历史对话做上下文，但 token 消耗太大。

后面主要写了两个方法：
1. **断点修复**：识别并修复最后一个不完整的用例
2. **续写**：把前 3 个用例作为格式参考，但不全量传，最多续写 5 次，5 次后还超长就返回当前结果，不无限循环。
